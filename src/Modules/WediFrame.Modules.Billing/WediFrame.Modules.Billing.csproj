<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <!-- Npgsql EF Core provider brings Microsoft.EntityFrameworkCore.Relational
         (ToTable, HasData and other relational extensions) — convention for every
         module that owns entities. -->
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <!-- Stripe Checkout + webhook (payment gateway adapter). -->
    <PackageReference Include="Stripe.net" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\WediFrame.Shared\WediFrame.Shared.csproj" />
  </ItemGroup>
</Project>
