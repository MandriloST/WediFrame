<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\..\WediFrame.Shared\WediFrame.Shared.csproj" />
    <!-- Retention only schedules; the work lives behind contracts:
         IEventRetention (Events) flips status + finalizes deletion, and
         IEventMediaPurge (Shared port, implemented by Media) erases the media.
         The purge port lives in Shared, so Retention needs no ref to Media. -->
    <ProjectReference Include="..\WediFrame.Modules.Events\WediFrame.Modules.Events.csproj" />
  </ItemGroup>
</Project>
